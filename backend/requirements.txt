# --- Web framework ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9        # required for UploadFile (POST /api/detections/analyze)
websockets==13.1

# --- Settings / config ---
pydantic==2.9.2
pydantic-settings==2.5.2

# --- Database ---
sqlalchemy==2.0.35
alembic==1.13.2
# psycopg2-binary==2.9.9         # PostgreSQL driver (matches DATABASE_URL in core/config.py)  # Disabled for SQLite

# --- Auth / security ---
python-jose[cryptography]==3.3.0
bcrypt

# --- Computer vision pipeline (app/cv) ---
# dlib==19.24.6
# opencv-python-headless==4.10.0.84
numpy>=2.0
insightface>=0.7.3          # ArcFace face embeddings via ONNX (no C++ compilation)
onnxruntime>=1.19.0         # ONNX runtime for insightface models

# --- Notifications (app/services/notification.py) ---
twilio==9.3.2                  # SMS escalation; omit if SMS alerts aren't needed

# --- Testing ---
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2                  # TestClient / async API testing
